export default class Card {
    constructor ({ name, link }, cardSelector) {
        this._name = name;
        this._link = link;
        this._cardSelector = cardSelector;
    }

    getView() {
        //get the card view
        //set event listners
        //return the card
    }
}

